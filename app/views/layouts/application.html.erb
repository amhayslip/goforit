<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Goforit" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Goforit" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <link href='http://fonts.googleapis.com/css?family=Italiana|Open+Sans+Condensed:300' rel='stylesheet' type='text/css'>
    <%= csrf_meta_tags %>
  </head>
  <body class="back">
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
       <%= render 'layouts/messages' %>

       <article class="container logobox">
        <div class="row">
          <div class="col-sm-3">
          </div>
          <div class="col-sm-6 logo">
          <h1 class="logo"> <%= link_to 'GoForIt', root_path %></h1>
          <h2 class="tagline">a community to get you there<h2>
        </div>
        <div class="col-lg-3">
        </div>
         </div>
       </article>
       <%= yield %>
    </main>
  </body>
</html>
